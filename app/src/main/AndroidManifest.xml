<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bistro_chat">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".head2"></activity>
        <activity android:name=".message_rv" />
        <activity android:name=".chatting_terminal" />
        <activity android:name=".contact_profile" />
        <activity android:name=".my_profile" />
        <activity android:name=".chat_container" />
        <activity android:name=".create_profile" />
        <activity android:name=".contact_container" />
        <activity android:name=".sign_up" />
        <activity android:name=".sign_in" />
        <activity android:name=".welcome_page">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE"></action>
                <action android:name="android.net.wifi.WIFI_STATE_CHANGED"></action>
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>